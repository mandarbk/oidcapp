spring:
  application:
    name: api-server
  security:
    oauth2:
      resourceserver:
        opaquetoken:
          client-id: api_server
          client-secret: api_server_secret
          introspection-uri: http://127.0.0.1:9080/oauth2/introspect

logging:
  level:
    org.springframework.security: TRACE